sudo: required
services: docker
language: python
python:
  - "3.3"
env:
 - TEST="pyflakes"
 - TEST="pep8"
 - TEST="build"
 - TEST="tarball"

matrix:
  allow_failures:
    - env: TEST="pep8"

script:
- docker run -P -v $(pwd):/mnt --rm fedora:rawhide "/bin/sh" "/mnt/ci_$TEST.sh"
